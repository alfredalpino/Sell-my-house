# Gallery images

Drop your house photos here. They are served at `/gallery/your-file.jpg`.

Supported formats: **JPG**, **PNG**, **WebP**, **AVIF**.

- File names become image alt text (`kitchen-view.jpg` → "kitchen view").
- Photos are sorted alphabetically.
- After adding or removing files, restart `npm run dev` (or rebuild for production).
